# Xcaciv.Isolation
A self-contained dot net 10 developer tooling that hides the plumbing and lets you start, stop, inspect, and configure windows containers without dependency on tools like VMs, Docker or Kubernetes.
